<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Book</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #eef2f3;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding: 40px 20px;
    }

    .form-container {
      background: #fff;
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 600px;
    }

    h1 {
      text-align: center;
      color: #2c3e50;
      margin-bottom: 30px;
      font-size: 2rem;
      border-bottom: 3px solid #3498db;
      padding-bottom: 10px;
    }

    .form-section {
      margin-bottom: 30px;
    }

    .form-section h2 {
      color: #3498db;
      margin-bottom: 15px;
      font-size: 1.3rem;
    }

    .form-group {
      margin-bottom: 20px;
    }

    label {
      display: block;
      margin-bottom: 8px;
      color: #555;
      font-weight: 500;
    }

    input, select, textarea {
      width: 100%;
      padding: 12px 15px;
      border: 1px solid #ddd;
      border-radius: 6px;
      font-size: 16px;
      transition: all 0.3s;
    }

    input:focus, select:focus, textarea:focus {
      border-color: #3498db;
      outline: none;
      box-shadow: 0 0 0 2px rgba(52, 152, 219, 0.2);
    }

    .btn {
      background: #3498db;
      color: white;
      border: none;
      padding: 14px 20px;
      font-size: 16px;
      font-weight: 600;
      border-radius: 6px;
      cursor: pointer;
      width: 100%;
      transition: background 0.3s;
      margin-top: 10px;
    }

    .btn:hover {
      background: #2980b9;
    }

    .file-upload-hint {
      display: block;
      font-size: 0.8rem;
      color: #777;
      font-weight: normal;
      margin-top: 4px;
    }

    .image-preview {
      margin-top: 10px;
    }

    .image-preview img {
      max-width: 100px;
      max-height: 100px;
      border: 1px solid #ddd;
      border-radius: 4px;
      padding: 5px;
    }

    @media (max-width: 500px) {
      .form-container {
        padding: 20px;
      }

      h1 {
        font-size: 1.6rem;
      }
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h1>Edit Book</h1>

    <form action="/editdata?id=<%=userupdate.id%>" method="post" enctype="multipart/form-data">
  

      <div class="form-section">
        <h2>Shipping Information</h2>

        <div class="form-group">
          <label for="name">Full Name</label>
          <input type="text" id="name" name="name" value="<%= userupdate.name %>" required>
        </div>

        <div class="form-group">
          <label for="email">Email Address</label>
          <input type="email" id="email" name="email" value="<%= userupdate.email %>" required>
        </div>

        <div class="form-group">
          <label for="phone">Phone Number</label>
          <input type="tel" id="phone" name="phone" value="<%= userupdate.phone %>" required>
        </div>

        <div class="form-group">
          <label for="address">Shipping Address</label>
          <textarea id="address" name="address" rows="3" required><%= userupdate.address %></textarea>
        </div>

        <div class="form-group">
          <label for="city">City</label>
          <input type="text" id="city" name="city" value="<%= userupdate.city %>" required>
        </div>

        <div class="form-group">
          <label for="zip">ZIP/Postal Code</label>
          <input type="text" id="zip" name="zip" value="<%= userupdate.zip %>" required>
        </div>

        <div class="form-group">
          <label for="country">Country</label>
          <select id="country" name="country" required>
            <option value="">Select Country</option>
            <option value="US" <%= userupdate.country === 'US' ? 'selected' : '' %>>United States</option>
            <option value="UK" <%= userupdate.country === 'UK' ? 'selected' : '' %>>United Kingdom</option>
            <option value="CA" <%= userupdate.country === 'CA' ? 'selected' : '' %>>Canada</option>
            <option value="AU" <%= userupdate.country === 'AU' ? 'selected' : '' %>>Australia</option>
            <option value="DE" <%= userupdate.country === 'DE' ? 'selected' : '' %>>Germany</option>
            <option value="FR" <%= userupdate.country === 'FR' ? 'selected' : '' %>>France</option>
            <option value="JP" <%= userupdate.country === 'JP' ? 'selected' : '' %>>Japan</option>
          </select>
        </div>
      </div>

      <div class="form-section">
        <h2>Shipping Method</h2>
        <div class="form-group">
          <select id="shipping" name="shipping" required>
            <option value="">Select Shipping Method</option>
            <option value="standard" <%= userupdate.shipping === 'standard' ? 'selected' : '' %>>Standard Shipping (5-7 business days) - $5.99</option>
            <option value="express" <%= userupdate.shipping === 'express' ? 'selected' : '' %>>Express Shipping (2-3 business days) - $12.99</option>
            <option value="priority" <%= userupdate.shipping === 'priority' ? 'selected' : '' %>>Priority Shipping (1 business day) - $19.99</option>
          </select>
        </div>
      </div>

      <div class="form-section">
        <h2>Payment Information</h2>

        <div class="form-group">
          <label for="cardname">Name on Card</label>
          <input type="text" id="cardname" name="cardname" value="<%= userupdate.cardname %>" required>
        </div>

        <div class="form-group">
          <label for="cardnumber">Card Number</label>
          <input type="text" id="cardnumber" name="cardnumber" value="<%= userupdate.cardnumber %>" required>
        </div>

        <div style="display: flex; gap: 20px;">
          <div class="form-group" style="flex: 1;">
            <label for="expiry">Expiry Date</label>
            <input type="text" id="expiry" name="expiry" value="<%= userupdate.expiry %>" required>
          </div>
          <div class="form-group" style="flex: 1;">
            <label for="cvv">CVV</label>
            <input type="text" id="cvv" name="cvv" value="<%= userupdate.cvv %>" required>
          </div>
        </div>
      </div>

      <div class="form-section">
        <h2>Additional Information</h2>
        <div class="form-group">
          <label class="file-upload-label" for="file">
            Upload Supporting Document (Optional)
            <span class="file-upload-hint">You can upload a PDF, JPG, or PNG file (max 5MB)</span>
          </label>
          <input type="file" id="file" name="image">
          <% if (userupdate.image) { %>
            <div class="image-preview">
              <p>Current Image:</p>
              <img src="<%= userupdate.image %>" alt="Current document">
            </div>
          <% } %>
        </div>
      </div>

      <button type="submit" class="btn">Update Book</button>
    </form>
  </div>
</body>
</html>